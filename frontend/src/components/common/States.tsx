import { AlertCircle, Inbox, LoaderCircle, RotateCcw } from 'lucide-react'

export function LoadingState({ label = 'Loading security data' }: { label?: string }) { return <div className="flex min-h-[180px] flex-col items-center justify-center gap-3 text-sm text-slate-500"><LoaderCircle className="animate-spin text-cyan" size={24} /><span>{label}</span></div> }
export function ErrorState({ onRetry, message = 'Unable to connect to CloudShield API.' }: { onRetry?: () => void; message?: string }) { return <div className="flex min-h-[180px] flex-col items-center justify-center gap-3 rounded-2xl border border-rose-100 bg-rose-50/50 p-8 text-center"><AlertCircle className="text-rose-500" size={24} /><p className="text-sm font-semibold text-slate-700">{message}</p>{onRetry && <button onClick={onRetry} className="inline-flex items-center gap-2 rounded-lg bg-slate-900 px-3 py-2 text-xs font-bold text-white hover:bg-slate-700"><RotateCcw size={14} /> Retry</button>}</div> }
export function EmptyState({ label = 'No data available yet.' }: { label?: string }) { return <div className="flex min-h-[180px] flex-col items-center justify-center gap-3 text-center text-sm text-slate-500"><Inbox size={25} className="text-slate-300" /><span>{label}</span></div> }
