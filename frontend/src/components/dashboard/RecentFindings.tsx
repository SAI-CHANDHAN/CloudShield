import { ArrowUpRight } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { Finding } from '../../types/cloudshield'
import { EmptyState } from '../common/States'
import { SeverityBadge, StatusBadge } from '../findings/SeverityBadge'
export function RecentFindings({ findings }: { findings: Finding[] }) { const recent = findings.slice(0, 4); return <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="mb-4 flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-cyan">Latest signals</p><h2 className="mt-1 font-display text-lg font-bold">Recent findings</h2></div><Link to="/findings" className="text-xs font-bold text-cyan hover:text-cyan/70">View all</Link></div>{recent.length === 0 ? <EmptyState label="No findings returned by the backend." /> : <div className="divide-y divide-slate-100">{recent.map((finding) => <div key={finding.finding_id} className="flex items-center gap-3 py-3 first:pt-1"><SeverityBadge severity={finding.severity} /><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-700">{finding.title}</p><p className="mt-1 truncate text-[11px] text-slate-400">{finding.resource_id} · {finding.rule_id}</p></div><StatusBadge status={finding.status} /><Link to={`/findings/${finding.finding_id}`} aria-label={`View ${finding.finding_id}`} className="text-slate-300 hover:text-cyan"><ArrowUpRight size={16} /></Link></div>)}</div>}</div> }
