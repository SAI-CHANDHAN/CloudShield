import { ArrowUpRight, Search } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useMemo, useState } from 'react'
import type { Finding, FindingStatus, Severity } from '../../types/cloudshield'
import { EmptyState } from '../common/States'
import { SeverityBadge, StatusBadge } from './SeverityBadge'

export function FindingTable({ findings }: { findings: Finding[] }) {
  const [query, setQuery] = useState(''); const [severity, setSeverity] = useState<'ALL' | Severity>('ALL'); const [status, setStatus] = useState<'ALL' | FindingStatus>('ALL')
  const filtered = useMemo(() => findings.filter((finding) => { const matchesQuery = [finding.finding_id, finding.title, finding.resource_id, finding.category, finding.rule_id].join(' ').toLowerCase().includes(query.toLowerCase()); return matchesQuery && (severity === 'ALL' || finding.severity === severity) && (status === 'ALL' || finding.status === status) }), [findings, query, severity, status])
  return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="relative w-full sm:max-w-xs"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search findings" aria-label="Search findings" className="w-full rounded-lg border border-slate-200 bg-slate-50 py-2 pl-9 pr-3 text-xs outline-none transition placeholder:text-slate-400 focus:border-cyan focus:ring-2 focus:ring-cyan/10" /></div><div className="flex gap-2"><select value={severity} onChange={(event) => setSeverity(event.target.value as 'ALL' | Severity)} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600 outline-none" aria-label="Filter by severity"><option value="ALL">All severities</option>{['CRITICAL','HIGH','MEDIUM','LOW','INFO'].map((item) => <option key={item}>{item}</option>)}</select><select value={status} onChange={(event) => setStatus(event.target.value as 'ALL' | FindingStatus)} className="rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600 outline-none" aria-label="Filter by status"><option value="ALL">All statuses</option>{['OPEN','RESOLVED','SUPPRESSED'].map((item) => <option key={item}>{item}</option>)}</select></div></div><div className="overflow-x-auto">{filtered.length === 0 ? <EmptyState label={findings.length ? 'No findings match these filters.' : 'No findings returned by the backend.'} /> : <table className="w-full min-w-[900px] text-left"><thead className="bg-slate-50/80 text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400"><tr><th className="px-5 py-3">Severity</th><th className="px-5 py-3">Finding</th><th className="px-5 py-3">Resource</th><th className="px-5 py-3">Category</th><th className="px-5 py-3">Rule</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Risk</th><th className="px-5 py-3">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map((finding) => <tr key={finding.finding_id} className="group hover:bg-slate-50/70"><td className="px-5 py-4"><SeverityBadge severity={finding.severity} /></td><td className="max-w-[280px] px-5 py-4"><p className="truncate text-sm font-semibold text-slate-800">{finding.title}</p><p className="mt-1 text-[11px] text-slate-400">{finding.finding_id}</p></td><td className="px-5 py-4 text-xs font-medium text-slate-600">{finding.resource_id}</td><td className="px-5 py-4 text-xs text-slate-500">{finding.category}</td><td className="px-5 py-4 font-mono text-xs text-slate-500">{finding.rule_id}</td><td className="px-5 py-4"><StatusBadge status={finding.status} /></td><td className="px-5 py-4"><span className="font-display text-sm font-bold text-slate-800">{finding.risk_score}</span></td><td className="px-5 py-4"><Link to={`/findings/${finding.finding_id}`} className="inline-flex items-center gap-1 text-xs font-bold text-cyan hover:text-cyan/70">View <ArrowUpRight size={13} /></Link></td></tr>)}</tbody></table>}</div></div>
}
