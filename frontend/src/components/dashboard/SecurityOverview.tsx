import { AlertTriangle, Cloud, ShieldCheck, Siren } from 'lucide-react'
import type { Resource, RiskSummary } from '../../types/cloudshield'

export function SecurityOverview({ summary, resources }: { summary: RiskSummary; resources: Resource[] }) { const metrics = [{ label: 'Total resources', value: resources.length, note: 'Across connected cloud', icon: Cloud, tint: 'text-cyan bg-cyan/10' }, { label: 'Open findings', value: summary.open_findings, note: 'Needs attention', icon: Siren, tint: 'text-orange-600 bg-orange-50' }, { label: 'Critical findings', value: summary.critical_count, note: 'Immediate priority', icon: AlertTriangle, tint: 'text-rose-600 bg-rose-50' }, { label: 'High findings', value: summary.high_count, note: 'Review this cycle', icon: ShieldCheck, tint: 'text-amber-600 bg-amber-50' }]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({ label, value, note, icon: Icon, tint }) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-xs font-medium text-slate-500">{label}</p><p className="mt-2 font-display text-3xl font-bold tracking-tight text-slate-900">{value}</p></div><div className={`grid h-9 w-9 place-items-center rounded-xl ${tint}`}><Icon size={17} /></div></div><p className="mt-4 text-[11px] text-slate-400">{note}</p></div>)}</div> }
