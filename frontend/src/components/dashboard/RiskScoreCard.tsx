import { ShieldAlert } from 'lucide-react'
import type { RiskSummary } from '../../types/cloudshield'

export function RiskScoreCard({ summary }: { summary: RiskSummary }) { const score = Math.max(0, Math.min(100, summary.overall_risk_score)); const color = score >= 70 ? '#f97316' : score >= 40 ? '#f4b740' : '#10b9c7'; return <div className="relative overflow-hidden rounded-2xl bg-ink p-6 text-white shadow-xl shadow-ink/10 sm:p-7"><div className="absolute -right-16 -top-24 h-64 w-64 rounded-full border border-white/[0.07]" /><div className="absolute -right-4 -top-12 h-40 w-40 rounded-full border border-white/[0.07]" /><div className="relative flex items-start justify-between"><div><div className="mb-5 flex items-center gap-2 text-xs font-semibold text-slate-400"><ShieldAlert size={16} className="text-cyan" /> Overall risk score</div><p className="font-display text-5xl font-bold tracking-tight">{score}<span className="ml-1 text-lg font-medium text-slate-500">/100</span></p><p className="mt-2 text-xs text-slate-400">Calculated from open findings</p></div><div className="relative grid h-28 w-28 place-items-center sm:h-32 sm:w-32"><svg className="absolute inset-0 h-full w-full -rotate-90" viewBox="0 0 120 120"><circle cx="60" cy="60" r="50" fill="none" stroke="rgba(255,255,255,.08)" strokeWidth="8" /><circle cx="60" cy="60" r="50" fill="none" stroke={color} strokeWidth="8" strokeLinecap="round" strokeDasharray={`${score * 3.14} 314`} /></svg><div className="text-center"><p className="font-display text-xl font-bold" style={{ color }}>{score < 40 ? 'Low' : score < 70 ? 'Guarded' : 'Elevated'}</p><p className="text-[9px] uppercase tracking-widest text-slate-500">posture</p></div></div></div><div className="relative mt-7 grid grid-cols-4 gap-2 border-t border-white/10 pt-5">{[['Open', summary.open_findings], ['Critical', summary.critical_count], ['High', summary.high_count], ['Medium', summary.medium_count]].map(([label, value]) => <div key={label as string}><p className="font-display text-lg font-bold">{value}</p><p className="text-[10px] text-slate-500">{label}</p></div>)}</div></div> }
