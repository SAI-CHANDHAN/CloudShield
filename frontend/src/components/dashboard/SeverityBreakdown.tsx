import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts'
import type { RiskSummary, Severity } from '../../types/cloudshield'

const items: { key: keyof RiskSummary; label: Severity; color: string }[] = [{ key: 'critical_count', label: 'CRITICAL', color: '#e85555' }, { key: 'high_count', label: 'HIGH', color: '#f97316' }, { key: 'medium_count', label: 'MEDIUM', color: '#f4b740' }, { key: 'low_count', label: 'LOW', color: '#2ba9c0' }, { key: 'info_count', label: 'INFO', color: '#94a3b8' }]
export function SeverityBreakdown({ summary }: { summary: RiskSummary }) { const data = items.map(({ key, label, color }) => ({ name: label, value: summary[key] as number, color })).filter((item) => item.value > 0); const total = items.reduce((sum, item) => sum + (summary[item.key] as number), 0); return <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="mb-2"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-cyan">Signal mix</p><h2 className="mt-1 font-display text-lg font-bold">Severity breakdown</h2></div>{total === 0 ? <div className="grid min-h-[210px] place-items-center text-sm text-slate-400">No findings to distribute</div> : <div className="flex items-center gap-5"><div className="h-[210px] w-1/2 min-w-[150px]"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={55} outerRadius={82} paddingAngle={3} stroke="none">{data.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip contentStyle={{ border: '0', borderRadius: '10px', fontSize: '12px' }} /></PieChart></ResponsiveContainer></div><div className="flex-1 space-y-3">{items.map(({ key, label, color }) => <div key={label} className="flex items-center justify-between text-xs"><span className="flex items-center gap-2 text-slate-500"><span className="h-2 w-2 rounded-full" style={{ backgroundColor: color }} />{label}</span><span className="font-display font-bold text-slate-800">{summary[key]}</span></div>)}</div></div>}</div> }
